# Legacy visual-reference configuration only.
# Never add a Crave API key or another private credential to browser variables.

# Optional location label used while studying the local UI and mock fallback.
NEXT_PUBLIC_LOCATION_ID=loc_primary_location_id

# Optional domain-restricted browser key for Maps UI study.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=AIzaSyYourMapsKey

